<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sevenpixel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.QuoteDetailActivity"
            android:label="@string/title_activity_quote_detail"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".utils.network_connection.NetworkRequest"
            android:exported="false" />

        <activity
            android:name=".dialog.SelectAnimalDialog"
            android:label="@string/title_activity_selecte_animal_dialog"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>